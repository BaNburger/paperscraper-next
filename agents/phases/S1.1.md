# Runbook S1.1

## Phase

- `phase_id`: S1.1
- `packet`: `pm/phases/S1_1_FOUNDATION.md`

## Objective

Bootstrap runtime and schema foundations without shipping user-facing features.

## Entry Checks

1. No prior phase dependency.
2. Stage 1 scope lock acknowledged.

## Command Sequence

1. `npm run lint:agents`
2. `npm run lint:docs`
3. `npm run lint:phase -- --phase=S1.1`
4. `node tools/phase-gate/verify-s1_1.mjs`
5. `npm run gate:phase -- --phase=S1.1`

## Expected Evidence

1. Bootstrap commands succeed.
2. Health checks are operational.
3. Base schema exists.
4. Runtime smoke validates healthy and degraded readiness paths.

## Fail-Fast Checks

1. Missing phase packet sections.
2. Boundary rule violations (PSN001-PSN003).
3. Runtime boot dependency failures.
4. Missing bounded probe timeout configuration (`HEALTH_PROBE_TIMEOUT_MS`).
