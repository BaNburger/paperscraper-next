# Runbook S1.EXIT

## Phase

- `phase_id`: S1.EXIT
- `packet`: `pm/phases/S1_EXIT_ACCEPTANCE_GATE.md`

## Objective

Verify all Stage 1 requirements and performance targets before Stage 2 starts.

## Entry Checks

1. `gate:phase` for S1.4 passed.
2. Full Stage 1 test scope available.

## Command Sequence

1. `npm run lint:agents`
2. `npm run lint:docs`
3. `npm run lint:phase -- --phase=S1.EXIT`
4. `npm run gate:phase -- --phase=S1.EXIT`
5. `npm run gate:stage1`
6. `node tools/phase-gate/verify-s1_exit.mjs --mode=runtime`

## Expected Evidence

1. Golden-path E2E pass from `verify-s1_exit`.
2. Stage 1 performance measurements (`firstScoreMs`, feed `p95`).
3. Acceptance artifact bundle under `artifacts/stage1-acceptance/latest`:
   - `summary.json`
   - `metrics.json`
   - `events.json`
   - `report.md`

## Fail-Fast Checks

1. Any missing Stage 1 gate output.
2. Unresolved critical reliability defects.
3. Regression against Stage 1 performance baseline.
4. Placeholder verifier usage instead of real S1.EXIT verification.
