# Runbook S1.4

## Phase

- `phase_id`: S1.4
- `packet`: `pm/phases/S1_4_PIPELINE_FRONTEND.md`

## Objective

Deliver the complete four-screen Stage 1 surface and manual pipeline workflow.

## Entry Checks

1. `gate:phase` for S1.3 passed.
2. Stage 1 IA lock (4 screens) is active.

## Command Sequence

1. `npm run lint:agents`
2. `npm run lint:docs`
3. `npm run lint:phase -- --phase=S1.4`
4. `npm run gate:phase -- --phase=S1.4`

## Expected Evidence

1. Feed filter/sort/cursor behavior plus non-blank loading/empty/error states.
2. Object detail and entity detail with linked intelligence payloads.
3. Pipeline board add/move/remove evidence with position consistency.
4. Feed and pipeline split-pane interaction evidence (streams/API-key tabs, board editor side pane).
5. Blocking UI smoke evidence from `verify-s1_4_ui --mode=runtime`.

## Fail-Fast Checks

1. New top-level screen additions.
2. Blank/error-only primary screens.
3. Non-atomic board reordering behavior.
